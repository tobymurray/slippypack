[package]
name = "slippypack-web"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Base WASM module for the slippypack PWA (URL-template raster path)"

[lints]
workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
slippypack-core = { path = "../slippypack-core" }
# Phase 4 dependencies — uncomment as the modules land:
# wasm-bindgen        = "0.2"
# wasm-bindgen-futures = "0.4"
# js-sys               = "0.3"
# web-sys              = { version = "0.3", features = ["FileSystemSyncAccessHandle", "Worker"] }
