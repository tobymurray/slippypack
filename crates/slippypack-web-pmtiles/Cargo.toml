[package]
name = "slippypack-web-pmtiles"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "PMTiles reader for slippypack-web; lazy-loaded WASM module"

[lints]
workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
slippypack-core = { path = "../slippypack-core" }
# Phase 6 dependencies — uncomment as the module lands:
# pmtiles      = "0.23"
# wasm-bindgen = "0.2"
