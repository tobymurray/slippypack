[package]
name = "slippypack-web-mbtiles"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "MBTiles reader for slippypack-web; lazy-loaded WASM module"

[lints]
workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
slippypack-core = { path = "../slippypack-core" }
# Phase 6 dependencies — uncomment as the module lands:
# rusqlite        = { version = ">=0.38", default-features = false }
# sqlite-wasm-rs  = { version = "*", features = ["precompiled"] }
# wasm-bindgen    = "0.2"
